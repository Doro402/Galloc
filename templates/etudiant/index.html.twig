{% extends 'base.html.twig' %}

{% block title %}Hello CEtudiantController!{% endblock %}

{% block centre %}

{# {{ render(controller(
	'App\\Controller\\SearchController::searchBar'
	)) }} #}

<h1 class="text-center mt-3">Liste des Chambres !
	</h1>

    <a href="{{ path('etudiant_new') }}" role="button" class=" btn .btn-lg btn-primary m-2">Creer Nouveau ! </a>
	<table class="table table-bordered ">
		<thead class=" thead-light">
			<tr>
				<th>Id</th>
				<th>matricule</th>
                <th>nom</th>
                <th> Actions</th>
			</tr>
		</thead>

		{% for etudiant in etudiants %}
			<tr>
				<td>{{  etudiant.id }}</td>
				<td>{{ etudiant.matricule }}</td>
                <td>{{ etudiant.nom }}</td>
                <td>
                <a href="{{ path('chambre_edit', {'id': etudiant.id}) }}" class="btn btn-success">edit</a>
                </td>
            </tr>
		{% else %}
            <tr>
                <td colspan="5">Pas d'Etudiants</td>
            </tr>
		{% endfor %}

	</table>
  <div class="navigation d-flex justify-content-center">
              {{ knp_pagination_render(etudiants) }}
             </div>

{# <style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ '/var/www/html/Galloc/src/Controller/ChambreController.php'|file_link(0) }}">src/Controller/ChambreController.php</a></code></li>
        <li>Your template at <code><a href="{{ '/var/www/html/Galloc/templates/chambre/index.html.twig'|file_link(0) }}">templates/chambre/index.html.twig</a></code></li>
    </ul>
</div> #}
{% endblock %}
